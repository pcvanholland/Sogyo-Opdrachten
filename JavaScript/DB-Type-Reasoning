Webshop -> Safety. (SQL)
- Only if multiple clients need to talk to the DB.

Availability of tickets -> No ratrace. (SQL)
- Can be done in NoSQL as well.

Events extendable -> Can need extra rows. (NoSQL)

Course -> Small scale. (No NoSQL)

Hence: No NoSQL.